name: 🎡 ci
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
jobs:
  check:
    name: 🔬 check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/setup_yarn
      - uses: reviewdog/action-setup@v1
      - run: npx spectral lint -f json invalid.yaml | node .github/workflows/to-rdjson.js | reviewdog -f=rdjson -name="🔬 check" -reporter=github-pr-review -fail-on-error
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
